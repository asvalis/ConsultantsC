<div class ="container">

  <br>
  <table class="table table-bordered">
    <thead>
        <tr class="registeredUsers">
          <td>First Name</td>
          <td>Last Name</td>
          <td>E-mail</td>
          <td></td>
          <td></td>
          <td></td>
        </tr>
    </thead>

    <% @users.each do |user| %>
        <tr>
          <td><%= user.firstname %></td>
          <td><%= user.lastname %></td>
          <td><%= user.email %></td>
          <td> <%= link_to "Delete User", user_path(user), :confirm => 'Are you sure you want to destroy this account?', :method => :delete , :class => "btn btn-signin"  %> </td>
        </tr>

    <% end %>

  </table>
   <%= link_to "Invite User", new_user_invitation_path %>

</div>